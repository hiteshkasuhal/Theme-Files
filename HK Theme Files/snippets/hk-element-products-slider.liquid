{%  assign counter_block = block %}
{% capture slider_config %}
{
  "dots": true,
  "arrows": false,
  "speed": 400,
  "slidesToShow": {{ block.settings.columns_desktop }},
  "slidesToScroll": 1,
  "autoplay": false,
  "infinite": true,
  "cssEase": "linear",
  "responsive": [
    {
      "breakpoint": 915,
      "settings": {
        "slidesToShow": {{ block.settings.columns_mobile }},
        "slidesToScroll": 1
      }
    }
  ]
}
{% endcapture %}
<div class="hk-element hk-element-slider-main {{ block.settings.custom_class }} hk-element-{{ block.id }}"{% if block.settings.custom_id != blank %} id="{{ block.settings.custom_id }}"{% endif %}>
<div class="hk-slick-slider" data-slider-config='{{ slider_config | strip }}'>
 {%- for block in section.blocks -%} 
            {%- case block.type -%}  
                {%- when 'product' -%} 
                    {%  render 'hk-product-item', block:block, counter_block:counter_block %} 
            {%  endcase %}
        {%  endfor %} 
</div>
</div>