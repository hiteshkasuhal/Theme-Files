<div class="el-element el-element-icon-with-text-grid-main {{ block.settings.custom_class }} el-element-{{ block.id }}"{% if block.settings.custom_id != blank %} id="{{ block.settings.custom_id }}"{% endif %}>
<div class="el-element-icon-with-text-grid">
    {% assign side = block.settings.position %}
 {%- for block in section.blocks -%} 
            {%- case block.type -%}  
                {%- when 'icon_with_text_item2' -%} 
                    {% if side != blank %}
            {% if block.settings.position == side %}
                    {%  render 'el-element-icon-with-text2', block:block %} 
            {%  endif %}
                    {%  else %}
                    {%  render 'el-element-icon-with-text2', block:block %} 
                    {%  endif %}
            {%  endcase %}
        {%  endfor %} 
</div>
</div>